package dsa.stack;

public class StackUsingQueues {

    public static void main(String[] args) {

        /**
         *
         * use 2 stacks when add operation of queue, it adds in rear to add, push in the stack 1
         * keep pushing if add operation to remove, it removes from front for removing, pop all the
         * elements except last from stack 1 and push into stack2 and revert the stack2 elements
         * back to stack1
         *
         *
         **/
    }
}
