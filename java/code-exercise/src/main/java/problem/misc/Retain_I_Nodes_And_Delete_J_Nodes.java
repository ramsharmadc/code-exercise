package problem.misc;

/**
 */
public class Retain_I_Nodes_And_Delete_J_Nodes {

	public static void main(String[] args) {
		/*
		 * Scanner in = new Scanner(System.in); String arrStr = in.nextLine(); String[]
		 * arr_S = arrStr.split(","); int[] arr_I = new int[arr_S.length];
		 * 
		 * for (int i = 0; i < arr_S.length; i++) { arr_I[i] =
		 * Integer.valueOf(arr_S[i]); }
		 * 
		 * final int I = Integer.valueOf(in.nextLine()); // to retain final int J =
		 * in.nextInt(); // to delete int newArrIdx = 0;
		 * 
		 * int[] arr_new = new int[arr_I.length]; int retained = 0; int deleted = 0;
		 * 
		 * for (int k = 0; k < arr_I.length; k++) { if (retained == I) { deleted += 1;
		 * if (deleted == J) { retained = 0; deleted = 0; } } else {
		 * arr_new[newArrIdx++] = arr_I[k]; retained += 1; } }
		 * 
		 * for (int l = 0; l < arr_new.length; l++) { if (arr_new[l] != 0) { arr_I[l] =
		 * arr_new[l]; } }
		 */
		System.out.println("RAM".substring(0, "RAM".length() - 1));

		// deleteElements(arr_I, i, j);
	}

	private static void deleteRange(int[] arr_i, int k, int j, int[] arr_new) {
		for (int m = 0; m < j; m++) {

		}
	}

	private static void deleteElements(int[] arr_I, int i, int j) {

	}

}
