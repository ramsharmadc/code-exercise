package lang.jdk8.interfaces;

public class SubClassOfAandB extends ClassAandB implements InterfaceA {

    // no need to implement Interface A's method, because
    // it inherits it frm ClassAandB which has already implemented it
}
